import styled from 'styled-components/native'
import { LayoutContainer } from '../LayoutContainer'

export const DifficultBadgeContainer = styled(LayoutContainer.Row)`
    height: 20px;
    padding-horizontal: 6px;
    border-radius: 10px;
    align-items: center;
    background-color: #d7d8de;
`